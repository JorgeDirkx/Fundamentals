package com.jorgedirkx.wrapperclasses;

public class App {
    public static void main(String[] args) {
        //wrappers; primitive data types are wrapped in objects; object edition of each data type

        Integer intObject = Integer.valueOf(5);
        System.out.println(intObject);

        //autoboxing: datatype to wrapping of object is auto: autoboxing !
    }
}
